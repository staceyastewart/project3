<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title></title>

</head>
  <body>
    <div id="body_index">
       <h1 class="intro"> </h1>
        <div id="allstuff">
          <div class="writing">
            <h2 id="welcome">Welcome To The Events Page</h2>
            <p>Here you will be able to search for an event that peaks your interest.<br>
            Also, if you have an event in mind, scroll down and create your own event.
            </div>

          <div class="listsearch">
            <%= search_form_for @q do |f| %>
              <%= f.label :name_cont, "Title Contains" %>
              <%= f.text_field :title_cont %>
              <%= f.submit "by keyword" %><br>
              <%= f.submit "Search all" %><br>
            <% end %>
            <div class="searchresults">
             <p> Search Results:</p>
             <ul class="event_ul">
            <% @results.each do |event| %>
               <li>
                <a href="/events/<%= event.id %>">Title: <%= event.title %></a>
                </li>
            <% end %>
            </ul>
              </div>
          </div>
        <div class="listevents">
          <p>Upcoming Events:</p><br>
            <img id="event_img" src=<%=image_path("Admit1.png") %>>
              <ul class="event_ul">
              <% @event.each do |x| %>
                  <li>
                    <a href="/events/<%= x['id'] %>">Title: <%= x['title'] %></a>
                  </li>
              <% end %>
               </ul>
        </div>
          <div class= "createEvent">
            <h1>Create an Event:</h1>
            <p> Click the link below to create your own event</p>
             <a href="/events/new" title="">Create an event</a>
          </div>
        </div>
      </div>
  </body>
</html>
