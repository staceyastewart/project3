<% if user_signed_in? %>

<div class="sticker">
  <div class="col-md-8 center"></div>
    <h1 class="hello">Welcome, <strong><%= current_user.name %></strong>.</h1>
      <img class="bg" src=<%=image_path("bg.jpg") %> alt="">
  </div>



<br>






<div id="cal_container">

<div id="calendar">
</div>
</div>

<left>
<div class="card">
<div class="row">
  <div class="col-xs-6 col-md-4">
      <% @favorites.each do |fav| %>
  <%= fav["title"] %>
  <%= fav["url"] %>
<% end %>
  </div>
  <div class="col-xs-6 col-md-4">  <% @fav_charity.each do |fav| %>
  <%= fav["name"] %>
  <%= fav["city"] %>
  <%= fav["url"] %>
<% end %></div>
  <div class="col-xs-6 col-md-4">  <% @fav_charity.each do |fav| %>
  <%= fav["name"] %>
  <%= fav["city"] %>
  <%= fav["url"] %>
<% end %></div>
</div>
</div>

<!--
  <div class="col-sm-4">
<%= @rand_news["title"] %>
<br>
<%= @rand_news["description"] %>
<br>
<%= @rand_news["url"] %>
  </div>


</div>
 -->



<% else %>
  <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %> |
  <%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %>
<div class="homepage-hero-module">
    <div class="video-container">
        <div class="filter"></div>
        <video autoplay loop class="fillWidth">
            <source src= <%=image_path("Draw-Something/MP4/Draw-Something.mp4") %>  type="video/mp4" />Your browser does not support the video tag. I suggest you upgrade your browser.
            <source src=<%=image_path("Draw-Something/WEBM/Draw-Something.webm") %> type="video/webm" />Your browser does not support the video tag. I suggest you upgrade your browser.
        </video>
        <div class="poster hidden">
            <!-- <img src=<%=image_path("Draw-Something/Snapshots/Draw-Something.jpg") %> alt="">
        </div>
    </div>
</div>

<% end %>


